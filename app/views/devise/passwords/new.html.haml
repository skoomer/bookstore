%main.container.general-main-wrap
  .text-center
    %h1.mb-30
      = t('forgot_password.forgot_your_password')
    %p.general-password-text
      = t('forgot_password.instructions_message')
  = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'general-form' }) do |f|
    .form-group.mb-25
      = f.input :email, class: 'form-control', placeholder: t('forgot_password.email_placeholder'), autofocus: true, label: false
    = f.submit t('forgot_password.email_instructions'), class: 'btn btn-default center-block mb-20'
    .text-center
      = link_to(t('forgot_password.cancel'), new_user_session_path, class: 'in-gold-500 mb-20')
